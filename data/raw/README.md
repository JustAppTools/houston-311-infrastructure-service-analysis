# Raw Data

Raw source extracts are generated by `scripts/fetch_data.py` and are ignored by git when they are bulky CSV, GeoJSON, or JSONL files.

The committed `fetch_metadata.json` records the source URL, query window, query filter, download timestamp, and record count used for the current V1 outputs.
